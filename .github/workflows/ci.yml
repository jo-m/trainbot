on: [push]
name: ci

jobs:
  go_build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v3
      - name: Run build
        run: make docker_build
      - name: List build artifacts
        run: ls -lah build/
      - name: Archive build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: binaries
          path: |
            build/
  go_lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v3
      - name: Run lint
        run: make docker_lint
  go_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v3
      - name: Run tests
        run: make docker_test
  go_test_more:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v3
      - name: Run more tests
        run: make docker_test_more
  go_bench:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v3
      - name: Run bench
        run: make docker_bench
  frontend_build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v3
      - name: Run checks and build
        run: |
          cd frontend
          make docker_build
      - name: List build artifacts
        run: ls -lah frontend/dist/
      - name: Archive build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: web-dist
          path: |
            frontend/dist/
